export

SERIES_CPU  = cortex-m4
SERIES_ARCH = armv7e-m+fp

ifeq (STM32F401xB, $(DEVICE))
    MAPPED_DEVICE = STM32F401xC
endif

ifeq (STM32F401xD, $(DEVICE))
    MAPPED_DEVICE = STM32F401xE
endif

ifeq (STM32F405xE, $(DEVICE))
    MAPPED_DEVICE = STM32F405xx
endif

ifeq (STM32F405xG, $(DEVICE))
    MAPPED_DEVICE = STM32F405xx
endif

ifeq (STM32F407xE, $(DEVICE))
    MAPPED_DEVICE = STM32F407xx
endif

ifeq (STM32F407xG, $(DEVICE))
    MAPPED_DEVICE = STM32F407xx
endif

ifeq (STM32F411xC, $(DEVICE))
    MAPPED_DEVICE = STM32F411xE
endif

ifeq (STM32F413xG, $(DEVICE))
    MAPPED_DEVICE = STM32F413xx
endif

ifeq (STM32F413xH, $(DEVICE))
    MAPPED_DEVICE = STM32F413xx
endif

ifeq (STM32F415xG, $(DEVICE))
    MAPPED_DEVICE = STM32F415xx
endif

ifeq (STM32F417xE, $(DEVICE))
    MAPPED_DEVICE = STM32F417xx
endif

ifeq (STM32F417xG, $(DEVICE))
    MAPPED_DEVICE = STM32F417xx
endif

ifeq (STM32F423xH, $(DEVICE))
    MAPPED_DEVICE = STM32F423xx
endif

ifeq (STM32F427xG, $(DEVICE))
    MAPPED_DEVICE = STM32F427xx
endif

ifeq (STM32F427xI, $(DEVICE))
    MAPPED_DEVICE = STM32F427xx
endif

ifeq (STM32F429xE, $(DEVICE))
    MAPPED_DEVICE = STM32F429xx
endif

ifeq (STM32F429xG, $(DEVICE))
    MAPPED_DEVICE = STM32F429xx
endif

ifeq (STM32F429xI, $(DEVICE))
    MAPPED_DEVICE = STM32F429xx
endif

ifeq (STM32F437xG, $(DEVICE))
    MAPPED_DEVICE = STM32F437xx
endif

ifeq (STM32F437xI, $(DEVICE))
    MAPPED_DEVICE = STM32F437xx
endif

ifeq (STM32F439xG, $(DEVICE))
    MAPPED_DEVICE = STM32F439xx
endif

ifeq (STM32F439xI, $(DEVICE))
    MAPPED_DEVICE = STM32F439xx
endif

ifeq (STM32F446xC, $(DEVICE))
    MAPPED_DEVICE = STM32F446xE
endif

ifeq (STM32F469xE, $(DEVICE))
    MAPPED_DEVICE = STM32F469xI
endif

ifeq (STM32F469xG, $(DEVICE))
    MAPPED_DEVICE = STM32F469xI
endif

ifeq (STM32F479xG, $(DEVICE))
    MAPPED_DEVICE = STM32F479xI
endif
